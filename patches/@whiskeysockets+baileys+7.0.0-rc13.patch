diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/socket.js b/node_modules/@whiskeysockets/baileys/lib/Socket/socket.js
index 6357f9e..a867ad4 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/socket.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/socket.js
@@ -744,7 +744,9 @@ export const makeSocket = (config) => {
         try {
             updateServerTimeOffset(node);
             await uploadPreKeysToServerIfRequired();
-            await sendPassiveIq('active');
+            // Psicólogos en Red: con markOnlineOnConnect=false registrar companion como passive
+            // (evita "siempre online" / silenciar pushes). Ver Baileys #2553 / PR #2558.
+            await sendPassiveIq(config.markOnlineOnConnect === false ? 'passive' : 'active');
             // After successful login, validate our key-bundle against server
             try {
                 await digestKeyBundle();
