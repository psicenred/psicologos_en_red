# Worker WhatsApp en Railway: Node 22, sin build de Next.js.
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm install --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = []

[start]
cmd = "npm run whatsapp:worker"
