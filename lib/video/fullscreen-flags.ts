/**
 * Pantalla completa móvil (videollamada) — perfil paciente.
 *
 * ## Rollback (sin revert de git)
 * Pon `VIDEO_MOBILE_FS_USE_PORTAL` en `false` y redespliega.
 * Vuelve al pseudo-fullscreen solo-CSS anterior (clases en body).
 *
 * ## Rollback total
 * `git revert` del commit de este cambio.
 */
export const VIDEO_MOBILE_FS_USE_PORTAL = true;
